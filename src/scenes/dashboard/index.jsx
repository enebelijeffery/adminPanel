import { Box } from "@mui/material"
import Header from "../../components/Header"

const Dashboard=()=>{
    return(
        
        <Box m={'20px'}>
            <Header title={'DashBoard'} subtitle={'welcome to your header'}/>
        </Box>
    )
}
export default Dashboard